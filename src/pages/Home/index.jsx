import React from 'react'
import classNames from 'classnames'

import './Home.scss'

const Home = () => {

  return(
    <section className={classNames('home')}>
      <h2>
        WELLCOME
      </h2>
    </section>
  )
}

export default Home